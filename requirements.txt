# Core dependencies
# For GPU support (V100 and other CUDA GPUs), install with:
#   pip install torch>=2.0.0 torchvision>=0.15.0 torchaudio>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
# For CPU only:
#   pip install torch>=2.0.0 torchaudio>=2.0.0
torch>=2.0.0
torchaudio>=2.0.0
timm>=0.9.0
soxr>=0.3.0
torchcodec>=0.0.1

# Audio processing
librosa>=0.10.0

# Model loading
huggingface_hub>=0.16.0

# Utilities
numpy>=1.21.0
